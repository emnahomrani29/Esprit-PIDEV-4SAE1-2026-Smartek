# ════════════════════════════════════════════════════════════════
# SPONSOR SERVICE - Deployment & Service
# ════════════════════════════════════════════════════════════════
# Dépendances: MySQL, Auth Service
# Port: 8093
# ════════════════════════════════════════════════════════════════

apiVersion: apps/v1
kind: Deployment
metadata:
  name: sponsor-service
  namespace: smartek
  labels:
    app: sponsor-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sponsor-service
  template:
    metadata:
      labels:
        app: sponsor-service
        tier: backend
    spec:
      serviceAccountName: smartek-backend-sa
      containers:
      - name: sponsor-service
        image: ghcr.io/votre-org/smartek-sponsor-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8093
          name: http
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://mysql:3306/smartek_sponsor?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        - name: AUTH_SERVICE_URL
          value: "http://auth-service:8081"
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8093
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8093
          initialDelaySeconds: 60
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: sponsor-service
  namespace: smartek
  labels:
    app: sponsor-service
spec:
  type: ClusterIP
  ports:
  - port: 8093
    targetPort: 8093
    protocol: TCP
    name: http
  selector:
    app: sponsor-service
