apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: smartek
spec:
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
      nodePort: 30090
  type: NodePort
