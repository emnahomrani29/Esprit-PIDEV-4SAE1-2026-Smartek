apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: smartek
spec:
  selector:
    app: grafana
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30092
  type: NodePort
