package com.smartek.offersservice.config;

/**
 * TestSecurityConfig — kept for compatibility but no longer used by controller tests.
 * Controller tests now use standalone MockMvc setup which bypasses Spring Security entirely.
 */
public class TestSecurityConfig {
    // No-op: standalone MockMvc tests don't need a Spring Security configuration.
}
