apiVersion: v1
kind: Secret
metadata:
  name: smartek-sponsor-secret
  namespace: smartek-production
  labels:
    app: smartek-sponsor
type: Opaque
stringData:
  # Database Credentials
  SPRING_DATASOURCE_USERNAME: "smartek_user"
  SPRING_DATASOURCE_PASSWORD: "change-me-in-production"
  
  # Email Configuration
  SPRING_MAIL_USERNAME: "noreply@smartek.com"
  SPRING_MAIL_PASSWORD: "change-me-in-production"
  SPRING_MAIL_HOST: "smtp.gmail.com"
  SPRING_MAIL_PORT: "587"
  
  # JWT Secret (if needed)
  JWT_SECRET: "change-me-to-a-secure-random-string"
